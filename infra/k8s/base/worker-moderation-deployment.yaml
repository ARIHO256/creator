apiVersion: apps/v1
kind: Deployment
metadata:
  name: creatorapp-backend-worker-moderation
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: creatorapp-backend
      app.kubernetes.io/component: worker-moderation
  template:
    metadata:
      labels:
        app.kubernetes.io/name: creatorapp-backend
        app.kubernetes.io/component: worker-moderation
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: worker-moderation
          image: ghcr.io/example/creatorapp-backend:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: creatorapp-backend-config
          env:
            - name: JOBS_WORKER_ENABLED
              value: "true"
            - name: JOBS_WORKER_ID
              value: "moderation"
            - name: JOBS_WORKER_QUEUES
              value: "moderation"
            - name: JOBS_WORKER_BATCH
              value: "100"
            - name: JOBS_WORKER_CONCURRENCY
              value: "25"
            - name: JOBS_WORKER_POLL_MS
              value: "1000"
            - name: JOBS_WORKER_BUSY_POLL_MS
              value: "0"
            - name: REALTIME_STREAM_SERVER_ENABLED
              value: "false"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: creatorapp-backend-secrets
                  key: database-write-url
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: creatorapp-backend-secrets
                  key: jwt-access-secret
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: creatorapp-backend-secrets
                  key: jwt-refresh-secret
            - name: AUTH_REGISTER_QUEUE_SECRET
              valueFrom:
                secretKeyRef:
                  name: creatorapp-backend-secrets
                  key: auth-register-queue-secret
          command: ["node", "dist/main.js"]
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "4"
              memory: "6Gi"
